.feature-branch-rules:
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_COMMIT_BRANCH == "develop"
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
    - if: $CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH =~ /^feature\/.*/


lint:compose:
  stage: lint
  extends: .feature-branch-rules
  allow_failure: false
  script:
    # Vérifier les fichiers docker (notamment compose.yml)

lint:hadolint:
  stage: lint
  extends: .feature-branch-rules
  allow_failure: false
  script:
    # Vérifier les fichiers docker (notamment compose.yml)

lint:yaml:
  stage: lint
  extends: .feature-branch-rules
  allow_failure: false
  script:
    # Vérifier les fichiers docker (notamment compose.yml)

lint:shell:
  stage: lint
  extends: .feature-branch-rules
  allow_failure: false
  script:
    # Vérifier les fichiers docker (notamment compose.yml)

lint:backend:
  stage: lint
  extends: .feature-branch-rules
  allow_failure: false
  script:
    # Vérifier les fichiers docker (notamment compose.yml)

lint:eslint:
  stage: lint
  extends: .feature-branch-rules
  allow_failure: false
  script:
    # Vérifier les fichiers docker (notamment compose.yml)
